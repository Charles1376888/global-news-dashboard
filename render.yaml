services:
  - type: web
    name: global-news-dashboard
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.x
      - key: DEEPSEEK_KEY
        sync: false
